-- Load Rayfield with error handling
local success, Rayfield = pcall(function()
    return loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
end)

if not success then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Error",
        Text = "Failed to load Rayfield UI",
        Duration = 5
    })
    return
end

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local localPlayer = Players.LocalPlayer
local OWNER_ID = 8114658601
local isOwner = (localPlayer.UserId == OWNER_ID)

-- Store all script users for notification tracking
local scriptUsers = {}

-- Function to notify all players using the script
local function notifyAllUsers(message, isKickNotification)
    for _, player in pairs(Players:GetPlayers()) do
        -- Only send to players who also have this script loaded
        -- We do this by checking if they have a specific value in their player
        if player:FindFirstChild("ScriptLoaded") then
            pcall(function()
                -- Use remote event to notify if exists, otherwise use core notifications
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = isKickNotification and "⚠️ KICK NOTIFICATION" or "📢 Script Alert",
                    Text = message,
                    Duration = 5
                })
            end)
        end
    end
end

-- Mark that this script is loaded for this player
local scriptLoadedTag = Instance.new("BoolValue")
scriptLoadedTag.Name = "ScriptLoaded"
scriptLoadedTag.Value = true
scriptLoadedTag.Parent = localPlayer

-- Notify everyone that someone joined
local joinMessage = "🔔 " .. localPlayer.Name .. " has joined the script! (ID: " .. localPlayer.UserId .. ")"
notifyAllUsers(joinMessage, false)

-- Also notify with chat message if possible
pcall(function()
    game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemMessages"):FindFirstChild("SayMessageRequest"):FireServer(joinMessage, "All")
end)

-- Check if Remote exists
local Remote = nil
pcall(function()
    Remote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Attack")
end)

if not Remote then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Error",
        Text = "Attack remote not found! Script may not work.",
        Duration = 5
    })
end

-- Create Window
local Window = Rayfield:CreateWindow({
    Name = "MineBAT| by こんにちは",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "by こんにちは",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "KillAuraConfig",
        FileName = "settings"
    },
    Discord = {
        Enabled = false,
        Invite = "",
        RememberJoins = true
    },
    KeySystem = false
})

local killAuraEnabled = false
local multiHitEnabled = false
local attackRange = 30
local attackDelay = 0.5

-- Weapon list
local weaponList = {"Iron Sword", "Golden Sword", "Diamond Sword", "Netherite Sword", 
                    "Iron Axe", "Golden Axe", "Diamond Axe", "Netherite Axe"}

-- Get current weapon
local function getCurrentWeapon()
    local character = localPlayer.Character
    if not character then return "Iron Sword" end
    
    local tool = character:FindFirstChildOfClass("Tool")
    if tool then
        return tool.Name
    end
    
    return "Iron Sword"
end

-- Distance check
local function isInRange(targetChar, myPos)
    if not targetChar then return false end
    local rootPart = targetChar:FindFirstChild("HumanoidRootPart")
    if not rootPart then return false end
    return (rootPart.Position - myPos).Magnitude <= attackRange
end

-- Attack function
local function attackTarget(targetChar)
    if not targetChar or not Remote then return end
    local weapon = getCurrentWeapon()
    pcall(function()
        Remote:FireServer(targetChar, 1, nil, weapon)
    end)
end

-- Get targets
local function getTargets()
    local targets = {}
    local myChar = localPlayer.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return targets end
    
    local myPos = myChar.HumanoidRootPart.Position
    
    -- Player targets
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") and isInRange(char, myPos) then
                table.insert(targets, char)
            end
        end
    end
    
    -- AI targets
    local aiFolder = Workspace:FindFirstChild("Ai")
    if aiFolder then
        local aiTargets = {"Minion Ai", "Juggernaut Ai", "Ai"}
        for _, aiName in ipairs(aiTargets) do
            local ai = aiFolder:FindFirstChild(aiName)
            if ai and ai:FindFirstChild("HumanoidRootPart") and isInRange(ai, myPos) then
                table.insert(targets, ai)
            end
        end
    end
    
    return targets
end

-- Auto Golden Apple
local autoGoldenAppleEnabled = false
local goldenAppleHealthThreshold = 50
local goldenAppleCooldown = false
local lastGoldenAppleTime = 0
local goldenAppleDelay = 5

local function useGoldenApple()
    if not autoGoldenAppleEnabled or goldenAppleCooldown then return end
    
    local now = tick()
    if now - lastGoldenAppleTime < goldenAppleDelay then return end
    
    local character = localPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end
    
    local healthPercent = (humanoid.Health / humanoid.MaxHealth) * 100
    
    if healthPercent <= goldenAppleHealthThreshold then
        local goldenApple = character:FindFirstChild("Golden Apple")
        if goldenApple and goldenApple:FindFirstChild("Data") then
            pcall(function()
                goldenApple.Data:FireServer(false, false)
                task.wait(0.1)
                goldenApple.Data:FireServer(false, true)
                task.wait(0.1)
                goldenApple.Data:FireServer(true)
            end)
            
            lastGoldenAppleTime = now
            goldenAppleCooldown = true
            task.spawn(function()
                task.wait(goldenAppleDelay)
                goldenAppleCooldown = false
            end)
        end
    end
end

-- KillAura loop
local lastAttack = 0
local killAuraConnection = nil

local function startKillAura()
    if killAuraConnection then killAuraConnection:Disconnect() end
    
    killAuraConnection = RunService.Heartbeat:Connect(function()
        if not killAuraEnabled then return end
        
        local now = tick()
        if now - lastAttack < attackDelay then return end
        
        local myChar = localPlayer.Character
        if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return end
        
        local targets = getTargets()
        if #targets > 0 then
            if multiHitEnabled then
                for _, target in pairs(targets) do
                    attackTarget(target)
                end
            else
                attackTarget(targets[math.random(1, #targets)])
            end
            lastAttack = now
        end
    end)
end

-- Auto Golden Apple loop
local goldenAppleConnection = nil

local function startAutoGoldenApple()
    if goldenAppleConnection then goldenAppleConnection:Disconnect() end
    goldenAppleConnection = RunService.Heartbeat:Connect(function()
        useGoldenApple()
    end)
end

-- ============ MAIN TAB ============
local MainTab = Window:CreateTab("Main", 4483362458)

local HitboxButton = MainTab:CreateButton({
    Name = "🔴 Load Hitbox Script",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://pastefy.app/ItfO0tdg/raw"))()
        end)
        HitboxButton:SetName("✅ Hitbox Script Loaded!")
        task.wait(2)
        HitboxButton:SetName("🔴 Load Hitbox Script")
    end
})

local Toggle = MainTab:CreateToggle({
    Name = "Toggle KillAura",
    CurrentValue = false,
    Flag = "ToggleKillAura",
    Callback = function(value)
        killAuraEnabled = value
        if value then
            startKillAura()
        end
    end
})

local MultiToggle = MainTab:CreateToggle({
    Name = "Multi-Hit (hit all targets)",
    CurrentValue = false,
    Flag = "MultiHitToggle",
    Callback = function(value)
        multiHitEnabled = value
    end
})

local RangeInput = MainTab:CreateInput({
    Name = "Attack Range (studs)",
    PlaceholderText = "30",
    Flag = "RangeInput",
    Callback = function(text)
        local num = tonumber(text)
        if num then attackRange = math.clamp(num, 5, 100) end
    end
})

local SpeedInput = MainTab:CreateInput({
    Name = "Attack Speed (seconds)",
    PlaceholderText = "0.5",
    Flag = "SpeedInput",
    Callback = function(text)
        local num = tonumber(text)
        if num then attackDelay = math.clamp(num, 0.1, 2) end
    end
})

-- ============ AUTO GOLDEN APPLE TAB ============
local GoldenAppleTab = Window:CreateTab("Auto Golden Apple", 4483362458)

local GoldenAppleToggle = GoldenAppleTab:CreateToggle({
    Name = "Toggle Auto Golden Apple",
    CurrentValue = false,
    Flag = "ToggleGoldenApple",
    Callback = function(value)
        autoGoldenAppleEnabled = value
        if value then
            startAutoGoldenApple()
        elseif goldenAppleConnection then
            goldenAppleConnection:Disconnect()
            goldenAppleConnection = nil
        end
    end
})

local HealthInput = GoldenAppleTab:CreateInput({
    Name = "Health Threshold (%)",
    PlaceholderText = "50",
    Flag = "HealthInput",
    Callback = function(text)
        local num = tonumber(text)
        if num then goldenAppleHealthThreshold = math.clamp(num, 1, 99) end
    end
})

local CooldownInput = GoldenAppleTab:CreateInput({
    Name = "Golden Apple Cooldown (seconds)",
    PlaceholderText = "5",
    Flag = "CooldownInput",
    Callback = function(text)
        local num = tonumber(text)
        if num then goldenAppleDelay = math.clamp(num, 1, 10) end
    end
})

-- ============ OWNER TAB (ONLY FOR OWNER) ============
if isOwner then
    local OwnerTab = Window:CreateTab("👑 Owner Control", 4483362458)
    
    OwnerTab:CreateLabel("👋 Welcome Owner @capmPH")
    OwnerTab:CreateLabel("Owner ID: 8114658601")
    OwnerTab:CreateLabel("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    
    -- Get script users (players who have ScriptLoaded value)
    local function getScriptUsers()
        local users = {}
        for _, player in pairs(Players:GetPlayers()) do
            if player:FindFirstChild("ScriptLoaded") then
                table.insert(users, player)
            end
        end
        return users
    end
    
    local PlayerListLabel = OwnerTab:CreateLabel("━━━ Script Users (Click refresh) ━━━")
    
    -- Function to update user list display
    local function updateUserListDisplay()
        local users = getScriptUsers()
        local userNames = {}
        for _, user in ipairs(users) do
            local tag = (user.UserId == OWNER_ID) and " 👑 (YOU)" or ""
            table.insert(userNames, user.Name .. tag)
        end
        if #userNames == 0 then
            PlayerListLabel:SetLabel("━━━ No other script users ━━━")
        else
            PlayerListLabel:SetLabel("━━━ Script Users: " .. #users .. " ━━━\n" .. table.concat(userNames, "\n"))
        end
    end
    
    -- Refresh button for user list
    local RefreshButton = OwnerTab:CreateButton({
        Name = "🔄 Refresh User List",
        Callback = function()
            updateUserListDisplay()
            updatePlayerDropdown()
            RefreshButton:SetName("✅ Refreshed!")
            task.wait(1)
            RefreshButton:SetName("🔄 Refresh User List")
        end
    })
    
    -- Player dropdown for kicking
    local playerOptions = {"Select a script user..."}
    local selectedPlayer = nil
    
    local function updatePlayerDropdown()
        local newOptions = {"Select a script user..."}
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= localPlayer and player:FindFirstChild("ScriptLoaded") then
                table.insert(newOptions, player.Name .. " (ID: " .. player.UserId .. ")")
            end
        end
        if #newOptions == 1 then
            table.insert(newOptions, "No other script users")
        end
        PlayerDropdown:SetOptions(newOptions)
        if #newOptions > 1 then
            PlayerDropdown:SetCurrentOption(newOptions[1])
        end
    end
    
    local PlayerDropdown = OwnerTab:CreateDropdown({
        Name = "Select Script User to Kick",
        Options = playerOptions,
        CurrentOption = playerOptions[1],
        Flag = "PlayerKickDropdown",
        Callback = function(option)
            selectedPlayer = option
        end
    })
    
    -- Kick button with notification to all users
    local KickButton = OwnerTab:CreateButton({
        Name = "🔨 KICK SELECTED PLAYER",
        Callback = function()
            if selectedPlayer and selectedPlayer ~= "Select a script user..." and selectedPlayer ~= "No other script users" then
                local playerName = selectedPlayer:match("^(.*) %(ID:") or selectedPlayer
                for _, player in pairs(Players:GetPlayers()) do
                    if player.Name == playerName and player:FindFirstChild("ScriptLoaded") then
                        -- Notify ALL script users about the kick
                        local kickMessage = "⚠️ " .. localPlayer.Name .. " (Owner) has kicked " .. player.Name .. " from the script!"
                        for _, notifyPlayer in pairs(Players:GetPlayers()) do
                            if notifyPlayer:FindFirstChild("ScriptLoaded") then
                                pcall(function()
                                    game:GetService("StarterGui"):SetCore("SendNotification", {
                                        Title = "🔨 PLAYER KICKED",
                                        Text = player.Name .. " was kicked by the script owner!",
                                        Duration = 5
                                    })
                                end)
                            end
                        end
                        
                        -- Also try to send chat message
                        pcall(function()
                            game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemMessages"):FindFirstChild("SayMessageRequest"):FireServer(kickMessage, "All")
                        end)
                        
                        -- Kick the player
                        pcall(function() 
                            player:Kick("Kicked by script owner @capmPH") 
                        end)
                        
                        KickButton:SetName("✅ Kicked " .. playerName .. "!")
                        task.wait(2)
                        KickButton:SetName("🔨 KICK SELECTED PLAYER")
                        task.wait(0.5)
                        updatePlayerDropdown()
                        updateUserListDisplay()
                        break
                    end
                end
            else
                -- Show error if no player selected
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Error",
                    Text = "Please select a script user to kick!",
                    Duration = 3
                })
            end
        end
    })
    
    OwnerTab:CreateLabel("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    
    -- Total script users count
    local TotalUsersLabel = OwnerTab:CreateLabel("Total script users: " .. #getScriptUsers())
    
    -- Auto refresh every 5 seconds
    task.spawn(function()
        while true do
            task.wait(5)
            pcall(function()
                TotalUsersLabel:SetLabel("Total script users: " .. #getScriptUsers())
                updateUserListDisplay()
            end)
        end
    end)
    
    -- Initial updates
    updateUserListDisplay()
    updatePlayerDropdown()
end

-- ============ INFO TAB ============
local InfoTab = Window:CreateTab("Info", 4483362458)

InfoTab:CreateLabel("✨ KillAura Script by @capmPH ✨")
InfoTab:CreateLabel("Targets: Players, Minion AI, Juggernaut AI, Ai")
InfoTab:CreateLabel("Weapons: All Swords & Axes")
InfoTab:CreateLabel("Type numbers directly into input boxes")
InfoTab:CreateLabel("Press 'Load Hitbox Script' for better hitbox detection")
InfoTab:CreateLabel("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
InfoTab:CreateLabel("📢 When someone executes this script,")
InfoTab:CreateLabel("   ALL script users will be notified!")
InfoTab:CreateLabel("👑 Owner can kick any script user")

-- Listen for new players joining the game (and using script)
Players.PlayerAdded:Connect(function(newPlayer)
    -- Check if they have the script loaded after a short delay
    task.wait(2)
    if newPlayer:FindFirstChild("ScriptLoaded") then
        local joinMsg = "🔔 " .. newPlayer.Name .. " is now using the script! (ID: " .. newPlayer.UserId .. ")"
        for _, player in pairs(Players:GetPlayers()) do
            if player:FindFirstChild("ScriptLoaded") then
                pcall(function()
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "New Script User",
                        Text = newPlayer.Name .. " joined the script!",
                        Duration = 4
                    })
                end)
            end
        end
    end
end)

-- Listen for players leaving
Players.PlayerRemoving:Connect(function(leavingPlayer)
    if leavingPlayer:FindFirstChild("ScriptLoaded") then
        local leaveMsg = "🔴 " .. leavingPlayer.Name .. " has stopped using the script!"
        for _, player in pairs(Players:GetPlayers()) do
            if player:FindFirstChild("ScriptLoaded") then
                pcall(function()
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "Script User Left",
                        Text = leavingPlayer.Name .. " left the script",
                        Duration = 4
                    })
                end)
            end
        end
    end
end)

-- Load configuration
pcall(function()
    Rayfield:LoadConfiguration()
end)

-- Success notification
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "KillAura Loaded",
    Text = "Welcome " .. localPlayer.Name .. "! Script by @capmPH",
    Duration = 3
})
